/*

© 2023 B1 Digital

User         : mehmetalicubukcu
Project Name : task_manager 
Name         : Mehmet Ali ÇUBUKÇU
Date         : 3.10.2023 22:19
Notes        : 

*/


import 'package:flutter/cupertino.dart';

class AppSize {
  AppSize._();

  static const double sizeS = 5.0;
  static const double sizeM = 10.0;
  static const double sizeL = 15.0;
  static const double sizeXL = 20.0;
  static const double sizeXXL = 25.0;

  static const EdgeInsets paddingS = EdgeInsets.all(sizeS);
  static const EdgeInsets paddingM = EdgeInsets.all(sizeM);
  static const EdgeInsets paddingL = EdgeInsets.all(sizeL);
  static const EdgeInsets paddingXL = EdgeInsets.all(sizeXL);
  static const EdgeInsets paddingXXL = EdgeInsets.all(sizeXXL);

  static const EdgeInsets paddingSHor = EdgeInsets.symmetric(horizontal: sizeS);
  static const EdgeInsets paddingMHor = EdgeInsets.symmetric(horizontal: sizeM);
  static const EdgeInsets paddingLHor = EdgeInsets.symmetric(horizontal: sizeL);
  static const EdgeInsets paddingXLHor = EdgeInsets.symmetric(horizontal: sizeXL);
  static const EdgeInsets paddingXXLHor = EdgeInsets.symmetric(horizontal: sizeXXL);

  static const EdgeInsets paddingSVer = EdgeInsets.symmetric(vertical: sizeS);
  static const EdgeInsets paddingMVer = EdgeInsets.symmetric(vertical: sizeM);
  static const EdgeInsets paddingLVer = EdgeInsets.symmetric(vertical: sizeL);
  static const EdgeInsets paddingXLVer = EdgeInsets.symmetric(vertical: sizeXL);
  static const EdgeInsets paddingXXLVer = EdgeInsets.symmetric(vertical: sizeXXL);

  static const EdgeInsets marginS = EdgeInsets.all(sizeS);
  static const EdgeInsets marginM = EdgeInsets.all(sizeM);
  static const EdgeInsets marginL = EdgeInsets.all(sizeL);
  static const EdgeInsets marginXL = EdgeInsets.all(sizeXL);
  static const EdgeInsets marginXXL = EdgeInsets.all(sizeXXL);

  static const EdgeInsets marginSHor = EdgeInsets.symmetric(horizontal: sizeS);
  static const EdgeInsets marginMHor = EdgeInsets.symmetric(horizontal: sizeM);
  static const EdgeInsets marginLHor = EdgeInsets.symmetric(horizontal: sizeL);
  static const EdgeInsets marginXLHor = EdgeInsets.symmetric(horizontal: sizeXL);
  static const EdgeInsets marginXXLHor = EdgeInsets.symmetric(horizontal: sizeXXL);

  static const EdgeInsets marginSVer = EdgeInsets.symmetric(vertical: sizeS);
  static const EdgeInsets marginMVer = EdgeInsets.symmetric(vertical: sizeM);
  static const EdgeInsets marginLVer = EdgeInsets.symmetric(vertical: sizeL);
  static const EdgeInsets marginXLVer = EdgeInsets.symmetric(vertical: sizeXL);
  static const EdgeInsets marginXXLVer = EdgeInsets.symmetric(vertical: sizeXXL);

  static const double radiusS = 5.0;
  static const double radiusM = 10.0;
  static const double radiusL = 15.0;
  static const double radiusXL = 20.0;
  static const double radiusXXL = 25.0;

  static const double iconSizeS = 5.0;
  static const double iconSizeM = 10.0;
  static const double iconSizeL = 15.0;
  static const double iconSizeXL = 20.0;
  static const double iconSizeXXL = 25.0;



}
